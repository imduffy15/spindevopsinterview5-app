# TODO: this whole file needs to be encrypted with sops and a AWS KMS key

secrets:
    default:
        files:
            application.yaml:
                data: |-
                    spring:
                      datasource:
                        url: jdbc:mysql://TODO:3306/notejam?useSSL=false
                        username: notejam
                        password: TODO
